# automata-lemma-arden
his project reads the description of a finite automaton from a structured text file, builds it in memory, verifies its coherence, and generates the regular expression it recognizes using **Arden’s Lemma**.
